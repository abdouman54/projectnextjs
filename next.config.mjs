/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost'], // Vous pouvez ajouter d'autres domaines si nécessaire
    },
  };
  
  export default nextConfig;
  